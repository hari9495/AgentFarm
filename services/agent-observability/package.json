{
    "name": "@agentfarm/agent-observability",
    "version": "0.1.0",
    "private": true,
    "type": "module",
    "scripts": {
        "build": "tsc -p tsconfig.json",
        "typecheck": "tsc -p tsconfig.json --noEmit",
        "test": "tsx --test src/**/*.test.ts"
    },
    "dependencies": {
        "@agentfarm/shared-types": "workspace:*",
        "@agentfarm/audit-storage": "workspace:*"
    },
    "devDependencies": {
        "tsx": "^4.20.6",
        "typescript": "^5.7.2"
    }
}
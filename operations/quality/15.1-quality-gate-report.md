# Quality Gate Report — Sprint 14 (2026-05-16)

**Sprint**: 14 — Developer Agent Gap Closure  
**Reporter**: CI / Copilot Agent  
**Gate Status**: ✅ PASSED

---

## Typecheck

| Package | Result | Notes |
|---------|--------|-------|
| `@agentfarm/api-gateway` | ✅ Clean | No errors |
| `@agentfarm/agent-runtime` | ✅ Clean | No errors |
| `@agentfarm/dashboard` | ✅ Clean | No errors |

---

## Tests

| Package | Pass | Fail | Total | Delta vs Sprint 13 |
|---------|------|------|-------|---------------------|
| `@agentfarm/api-gateway` | 1237 | 0 | 1237 | +10 |

---

## Test Suite Additions

**New suites (both in `@agentfarm/api-gateway`):**

- `GET /v1/workspaces/:workspaceId/pull-requests` — 5 tests  
  - empty list, all drafts, status filter, limit param, auth guard  
- `GET /v1/workspaces/:workspaceId/ci-failures (list)` — 5 tests  
  - empty list, all reports, status filter (fixed: triage runs sync → `complete`), limit param, auth guard

**Key correctness fix in CI failures suite:**  
`runTriage()` runs synchronously inside the intake handler and immediately sets report status to `'complete'`. The failing test was asserting `status=queued → total=1` (wrong). Fixed to `status=complete → total=1`.

---

## Scope of Changes

| File | Type | Description |
|------|------|-------------|
| `apps/agent-runtime/src/local-workspace-executor.ts` | Modified | `workspace_ai_code_review` real static analysis |
| `apps/api-gateway/src/routes/pull-requests.ts` | Modified | Added `listDrafts`, `GET /v1/workspaces/:workspaceId/pull-requests` |
| `apps/api-gateway/src/routes/ci-failures.ts` | Modified | Added `listReports`, `GET /v1/workspaces/:workspaceId/ci-failures` |
| `apps/dashboard/app/api/agents/[botId]/pr-drafts/route.ts` | New | Proxy GET to API gateway pull-requests list |
| `apps/dashboard/app/api/agents/[botId]/ci-runs/route.ts` | New | Proxy GET to API gateway ci-failures list |
| `apps/api-gateway/src/routes/pull-requests.test.ts` | Modified | 5 new list endpoint tests |
| `apps/api-gateway/src/routes/ci-failures.test.ts` | Modified | 5 new list endpoint tests + fix |

---

## Sign-off

All gate criteria met. Sprint 14 is clear for merge.

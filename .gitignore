node_modules/
dist/
.next/
coverage/

# Claude AI internal worktrees and session data
.claude/

# Environment variable files — real values must never be committed
.env
.env.*
!.env.example
!.env.production.example

# Private keys and certificates
*.pem
*.key
*.p12
*.pfx
*.jks
*.cer
*.crt
serviceAccountKey.json

# Secret directories
secrets/
.secrets/

# VS Code MCP config (may contain API keys via env)
.vscode/mcp.json

*.log

# Generated test artifacts and local dev script output
scripts/rpa-screenshots/
packages/e2e/playwright-report/
packages/e2e/test-results/
apps/website/.wrangler/
cloudflared.exe

# Local dev SQLite auth DB (next-auth) — must never be committed
apps/website/.auth.sqlite
apps/website/.auth.sqlite-shm
apps/website/.auth.sqlite-wal

# Scratch / scriptable dumps (not source)
read.md
routes_raw.txt

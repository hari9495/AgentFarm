# Copy this to terraform.tfvars (gitignored) for local terraform runs.
# In CI, these are injected via TF_VAR_* environment variables from GitHub Secrets.

cloudflare_api_token = "your-cloudflare-api-token"
cloudflare_account_id = "your-cloudflare-account-id"
zone_name             = "agentfarms.in"
